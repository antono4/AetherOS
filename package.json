{
  "name": "aether-os-spatial-canvas",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host",
    "build": "tsc && vite build && cp public/*.html dist/ && cp -r public/docs dist/docs/ && cp public/favicon.svg dist/ && mv dist/index.html dist/app.html && cp public/index.html dist/index.html && cp public/index.html dist/404.html",
    "preview": "vite preview"
  },
  "dependencies": {
    "three": "^0.160.0"
  },
  "devDependencies": {
    "typescript": "^5.3.3",
    "vite": "^5.0.12",
    "@types/three": "^0.160.0"
  }
}
